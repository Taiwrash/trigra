apiVersion: v1
kind: ServiceAccount
metadata:
  name: trigra
  namespace: default
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: trigra
rules:
  # Allow all operations on all resources for GitOps
  - apiGroups: ["*"]
    resources: ["*"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: trigra
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: trigra
subjects:
  - kind: ServiceAccount
    name: trigra
    namespace: default
