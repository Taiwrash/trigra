# Environment Configuration Template
# Copy this file to .env and fill in your values

# GitHub Personal Access Token (optional for public repos, required for private)
# Create at: https://github.com/settings/tokens
# Permissions needed: repo (for private repos) or public_repo (for public repos)
GITHUB_TOKEN=your_github_token_here

# GitHub Webhook Secret
# Set this to a random string and use the same value when configuring the webhook
# Generate with: openssl rand -hex 32
WEBHOOK_SECRET=your_webhook_secret_here

# Server Configuration
SERVER_PORT=8082

# Kubernetes Namespace (default: default)
NAMESPACE=default
